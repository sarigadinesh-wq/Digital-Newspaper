services:
  newspaper:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: digital_newspaper_app
    env_file:
      - .env
    ports:
      - "${FRONTEND_PORT:-80}:8000"
    restart: unless-stopped
